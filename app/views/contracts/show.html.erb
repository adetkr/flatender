    <% if @contract.match.flat1.user == current_user %>
      <% current_user_flat = @contract.match.flat1 %>
      <% other_user_flat = @contract.match.flat2 %>
    <% else %>
      <% current_user_flat = @contract.match.flat2 %>
      <% other_user_flat = @contract.match.flat1 %>
    <% end %>

<section class="mt-5 pt-5">
  <div class="container">
    <div class="row">
      <div class="col-6">
        <%= image_tag "pdf_generate", width: 550, height: 550, class: "rounded" %>
      </div>
      <div class="col-6 p-3">
        <h3> Congratulations <span class="text-primary"><%=current_user.name%></span></h3>
        <div class="bg-white-smoke p-4 my-2 rounded-lg">
          <p><span><%= other_user_flat.user.name %></span> wants also to exchange his flat with yours</p> 
            <p>Just one more step and you'll be ready to live and study abroad</p>
            <p>Here you are the terms of the contract beetween you and <%= other_user_flat.user.name %></span></p>  
              <h5>If both of you sign this contract : </h5>
              <p>Your next address will be : <%= other_user_flat.address %></p> 
              <p>You agree to rent your flat to <%= other_user_flat.user.name %> at the price of euros <%= current_user_flat.rent %> euros</p>
              <p><%=other_user_flat.user.name %> rents his flat at the price of euros <%= other_user_flat.rent %> euros</p>
              <h5>In that case :</h5>
                <% if other_user_flat.rent > current_user_flat.rent %>
                <% diff = other_user_flat.rent - current_user_flat.rent %>
                <p> You agree to settle to <%= other_user_flat.user.name %> every renting month the amount of euros <%= diff %></p>
                <% else %>
                <% diff = current_user_flat.rent - other_user_flat.rent %>
                <p><%= other_user_flat.user.name %></span> agrees to settle to you every renting month the amount of euros <%= diff %></p>
                <% end  %>
              <p>Your exchange will start around September 2021 for a Year</p>
        </div> 
        <div class="d-flex justify-content-start align-items-center">
          <%= link_to 'Generate pdf', pdf_path(@contract), :format => :pdf, :target=>'blank', class: "btn-style-01 btn-lg mr-3 p-3 text-white rounded text-uppercase" %>
          <p class="text-primary text-uppercase">sign your contract</p>
        </div>    
      </div>
    </div>    
  </div>  
</section>



